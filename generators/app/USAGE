Example usages:

yo norgate-av # Create a project in a folder with the project's name as prompted in the generator.
yo norgate-av . -o # Create a project in current folder and open with Visual Studio Code.
yo norgate-av Hello -t=ts -y # Create a TypeScript project in './Hello', skip prompts, use defaults.
yo norgate-av Hello -t=js -g # Create a TypeScript project in './Hello', initializes a git repository.
